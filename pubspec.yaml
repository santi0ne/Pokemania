name: pokemania
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.5.3

dependencies:
  flutter:
    sdk: flutter
  dartz: ^0.10.1
  http: ^1.2.2
  #bloc: ^8.1.4
  flutter_bloc: ^8.1.6
  meta: ^1.15.0
  get_it: ^8.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
